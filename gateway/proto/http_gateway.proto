syntax = "proto2";

package us3_turbo_access.gateway;

option cc_generic_services = true;

message GatewayHttpRequest {
}

message GatewayHttpResponse {
}

service GatewayHttpService {
  rpc default_method(GatewayHttpRequest) returns (GatewayHttpResponse);
}
